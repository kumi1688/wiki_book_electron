const express = require('express')
const bodyPraser = require('body-parser')
const app = express()

// Express가 body-parser 미들웨어를 적용하였다
app.use(bodyParser.urlencoded({ extended: false })) 

app.post('/', (req, res) => {
	console.log(req.body) // body-parser가 없는 경우 req.body를 읽지 못한다 
})
